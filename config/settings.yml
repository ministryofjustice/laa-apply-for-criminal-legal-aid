feature_flags:
  developer_tools:
    local: true
    staging: true
    production: false
  u18_ioj_passport:
    local: true
    staging: false
    production: false
  datastore_submission:
    local: true # if `true`, ensure the datastore service is running locally
    test: false # enable once we start writing tests for the feature
    staging: false
    production: false

# NOTE: consider if the setting you are adding here
# should belong in the k8s `config_map`, which is
# usually the case if it varies per environment.
settings:
  eforms_url: https://portal.legalservices.gov.uk
  session_timeout_minutes: 60 # not used yet, just an example
