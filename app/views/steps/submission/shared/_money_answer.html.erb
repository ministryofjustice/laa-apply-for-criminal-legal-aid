<%
  content_for(:row_question, flush: true) do
    t("summary.questions.#{money_answer.question}.question")
  end

  content_for(:row_answer, flush: true) do
    number_to_currency(money_answer.value, unit: '£', separator: '.', delimiter: ',') if money_answer.value
  end
%>

<%= render partial: 'steps/submission/shared/summary_row',
           locals: { row: money_answer, editable: editable } %>
